import styled from '@emotion/styled';

export const Styled = {
    Input: styled.input`
        width: 100%;
        border: 1px solid #eeeeee;
        padding: 1rem;
        border-radius: 0.5rem;
    `
}